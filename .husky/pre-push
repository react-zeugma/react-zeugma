#!/bin/sh
# Husky pre-push hook to prevent direct pushes to master branch

current_branch=$(git symbolic-ref --short HEAD)

if [ "$current_branch" = "master" ]; then
  echo "❌ Error: Direct pushes to the 'master' branch are prohibited!"
  echo "Please create a feature branch, push it, and open a Pull Request."
  exit 1
fi

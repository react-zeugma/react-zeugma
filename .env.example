# Copy to .env (never commit .env).
# NPM_TOKEN must be a Granular token with "Bypass 2FA" OR a Classic Automation token.
NPM_TOKEN=
# Optional: 6-digit code from your authenticator if publish asks for OTP
# NPM_OTP=

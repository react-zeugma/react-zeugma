# Local installs go through Nexus (matches ~/.npmrc in this environment).
registry=http://localhost:8081/repository/npm-all/

# Publishing uses publishConfig.registry in package.json (registry.npmjs.org).
# Set NPM_TOKEN to an npmjs automation token for `npm publish` / CI release.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
